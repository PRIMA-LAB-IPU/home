'use strict';
const canvas=document.querySelector('#cloud'),ctx=canvas.getContext('2d'),rotation=document.querySelector('#rotation');
let angle=25,spinning=false,lastTime=0,drag=null;
function drawCloud(){const r=canvas.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);canvas.width=r.width*d;canvas.height=r.height*d;ctx.setTransform(d,0,0,d,0,0);const w=r.width,h=r.height,cx=w/2,cy=h/2,R=Math.min(w*.38,h*.43);ctx.clearRect(0,0,w,h);ctx.strokeStyle='#a3bac018';ctx.lineWidth=1;for(let i=0;i<7;i++){ctx.beginPath();ctx.moveTo(w*.1,h*.2+i*h*.1);ctx.lineTo(w*.9,h*.2+i*h*.1);ctx.stroke()}const a=angle*Math.PI/180,points=[];for(let j=1;j<34;j++){const p=j/34*Math.PI;for(let i=0;i<68;i++){const t=i/68*Math.PI*2,x=Math.sin(p)*Math.cos(t),y=Math.cos(p),z=Math.sin(p)*Math.sin(t);const xx=x*Math.cos(a)+z*Math.sin(a),zz=-x*Math.sin(a)+z*Math.cos(a),yy=y*.93-zz*.28,depth=zz*.93+y*.28;points.push({x:cx+xx*R*(1+depth*.12),y:cy+yy*R,z:depth,i,j})}}points.sort((a,b)=>a.z-b.z);for(const p of points){const bright=(p.z+1)/2;ctx.fillStyle=p.i%12<4?`rgba(214,255,104,${.14+bright*.78})`:`rgba(112,212,223,${.1+bright*.68})`;ctx.beginPath();ctx.arc(p.x,p.y,.7+bright*1.25,0,Math.PI*2);ctx.fill()}ctx.strokeStyle='#d6ff6860';ctx.beginPath();ctx.moveTo(cx-R-20,cy);ctx.lineTo(cx+R+20,cy);ctx.stroke();ctx.fillStyle='#afc2c2';ctx.font='11px monospace';ctx.fillText('−X',cx-R-20,cy+20);ctx.fillText('+X',cx+R+5,cy+20)}
rotation.addEventListener('input',()=>{angle=+rotation.value;drawCloud()});new ResizeObserver(drawCloud).observe(canvas);canvas.addEventListener('pointerdown',e=>{drag=e.clientX;canvas.setPointerCapture(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(drag===null)return;angle=(angle+(e.clientX-drag)*.6+360)%360;drag=e.clientX;rotation.value=Math.round(angle);drawCloud()});['pointerup','pointercancel'].forEach(ev=>canvas.addEventListener(ev,()=>drag=null));
document.querySelector('#motion').addEventListener('click',e=>{spinning=!spinning;e.currentTarget.setAttribute('aria-pressed',String(spinning));if(spinning){lastTime=0;requestAnimationFrame(frame)}});function frame(t){if(!spinning)return;if(lastTime)angle=(angle+(t-lastTime)*.008)%360;lastTime=t;rotation.value=Math.round(angle);drawCloud();requestAnimationFrame(frame)}
